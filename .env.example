DATABASE_PATH=./db/job_tracker.db
# PORT is read as a shell env var by start.sh (PORT=8080 ./start.sh), not from
# this file — setting it here has no effect on the ./start.sh path.
PORT=9000
# Target number of applications to submit per week; shown on the dashboard
WEEKLY_TARGET=25
